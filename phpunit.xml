<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         cacheDirectory=".phpunit.cache"
         colors="true"
         failOnWarning="true"
         failOnRisky="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>Tests/Unit</directory>
        </testsuite>
    </testsuites>

    <!--
        Both halves of the module are measurable: the doubles in Tests/Stubs
        let the provider be booted and its hooks invoked for real, so it is
        covered by ProviderHooksTest rather than by reading its source.
    -->
    <source>
        <include>
            <directory suffix=".php">Providers</directory>
            <directory suffix=".php">Services</directory>
        </include>
    </source>
</phpunit>
